# Node.js
node_modules/

# Log files
npm-debug.log*
yarn-debug.log*
yarn-error.log*
*.log

# Environment variables
.env
.env.development.local
.env.test.local
.env.production.local
.env.local
!.env.example # Si tienen un archivo .env.example para mostrar la estructura

# Build output (if you transpile backend code, e.g., TypeScript)
/build/
/dist/

# Prisma specific
prisma/migrations/ # A veces se ignoran los archivos de migración generados, pero **muchas veces se quieren versionar**
                   # Si deciden versionar las migraciones, remuevan esta línea.
                   # En la mayoría de los casos de equipos, SÍ se versionan las migraciones.

# Editor directories and files
.idea/
.vscode/ # Configuración del editor VS Code
.DS_Store # Archivos de macOS
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
/generated/prisma
