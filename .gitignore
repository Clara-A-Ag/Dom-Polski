# Directorios de módulos de Node.js a nivel de proyecto (si aplica)
# Generalmente, los node_modules están dentro de frontend/ y backend/
# pero si por alguna razón aparece uno en la raíz, esto lo ignoraría.
node_modules/

# Logs del sistema o generales
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Archivos de configuración del sistema operativo (macOS)
.DS_Store

# Archivos de la IDE (ej. de VS Code a nivel de workspace)
.vscode/
.idea/

# Archivos de entorno (si hay alguno que manejen globalmente en la raíz)
.env
.env.local

# Carpeta de sus viejos archivos estáticos (si no los van a integrar directamente)
# Si mueven 'Dompo_santa_fe' a frontend/public, esta linea no sería necesaria aquí.
Dompo_santa_fe/